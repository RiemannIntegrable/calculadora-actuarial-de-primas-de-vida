source("basics.R")

IA <- function(tabla_mortalidad,r,i,x) {
    w <- max(tabla_mortalidad$x)
    if(w-x <= 0) return(0)

    k_vals <= 0:(w-x)
    for(k in k_vals) {
        a
    }

    return(IrAx)
}